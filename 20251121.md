```
useEffect(() => {
  fetchFromAPI(`channels?part=snippet&id=${id}`)
    .then((data) => )
}, [id])
```
These codes mean that once ```[id]``` was changed, 
```
  fetchFromAPI(`channels?part=snippet&id=${id}`)
    .then((data) => )
```
will be conducted. AKA once you got an new id, you just fetch data again.